import { JournalEvents } from "cmdr-journal";

export enum DBStore {
    missionAccepted = "MissionAccepted",
    completedJournalFiles = "completedJournalFiles",
    factions = "factions",
    currentState = "currentState",
    ships = "ships",
    materials = "materials",
    appSettings = "appSettings"
}